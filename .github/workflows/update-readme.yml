name: Update README

on:
  schedule:
    - cron: "0 0 * * *" # Runs every day at midnight
  workflow_dispatch: # Allows manual triggering

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Update README
        run: echo -e "\nLast updated - $(date)" >> README.md

      - name: Commit changes
        run: |
          git config --global user.name 'keston-4'
          git config --global user.email 'kurt.weston97@gmail.com'
          git add README.md
          git commit -m "Update README with last updated date"
          git push
